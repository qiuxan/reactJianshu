import React, { Component } from "react";
import { WritterWrapper } from "../style";

class Writer extends Component {
  render() {
    return <WritterWrapper>Writer</WritterWrapper>;
  }
}

export default Writer;
